import { resolve } from "path";

export default {
  mode: "development",
  build: {
    target: "ESNext",
    minify: false,
    lib: {
      entry: resolve(__dirname, "src/index.ts"),
      name: "mu",
      fileName: "mustang"
    }
  },
  esbuild: {
    target: "ESNext",
  },
  optimizeDeps:{
    esbuildOptions: {
      target: "ESNext",
    }
  }
};
